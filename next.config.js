// This file runs the the default configuration of Next.js but you can customise it to your needs. 
// For example, you can add custom webpack configuration, environment variables, or other settings that affect the build process.
// Next.js documentation: https://nextjs.org/docs/api-reference/next.config.js/introduction

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  output: 'export',
}

module.exports = nextConfig